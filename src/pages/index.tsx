import Head from 'next/head'

import Layout from '../components/Layout'

const Home: React.FC = () => {
  return (
    <div>
      <Head>
        <title>Fiona's Pet Shop</title>
      </Head>
      <Layout />
    </div>
  )
}

export default Home
